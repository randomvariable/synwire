# Data Model: GoReleaser MCP Server Binaries & Book Publishing Fix

This feature introduces no persistent data model. It is entirely CI/CD pipeline configuration.

The "entities" are build-time artifacts defined by naming conventions (see `contracts/release-artifacts.md`).
