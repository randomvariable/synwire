# synwire-lsp

Language Server Protocol integration for the Synwire AI agent framework.

Wraps `async-lsp` to provide a high-level `LspClient`, capability-conditional
agent tools, a language server registry, and an `LspPlugin` for the synwire
agent runtime.
